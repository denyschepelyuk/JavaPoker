import core.Card;
import core.Deck;
import core.Game;
import core.Player;

public class Main {
    public static void main(String[] args) {
        Game game = new Game();
        game.run(3);
    }
}